[build]
builder = "dockerfile"

[deploy]
healthcheckPath = "/setup/healthz"
healthcheckTimeout = 300
restartPolicyType = "on_failure"

# Require a persistent volume mounted at OpenClaw's default state dir (~/.openclaw).
requiredMountPath = "/home/node/.openclaw"
